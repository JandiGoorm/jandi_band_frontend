import DefaultLayout from "@/layouts/defaultLayout/DefaultLayout";

const MyPage = () => {
  return (
    <DefaultLayout>
      <main>
        <section>기본정보</section>

        <section>참여 팀 목록</section>

        <section>내 시간표 관리</section>
      </main>
    </DefaultLayout>
  );
};

export default MyPage;
